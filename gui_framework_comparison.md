# Windows 11 시스템 트레이 앱 전환을 위한 GUI 프레임워크 비교

현재 `dslr-filetransfer` 프로젝트는 Flask를 이용한 로컬 웹 서버와 시스템의 기본 웹 브라우저를 통해 UI를 제공하고 있습니다. 이를 완벽한 Windows 11 시스템 트레이 기반의 데스크톱 앱으로 전환하기 위해 고려할 수 있는 다양한 GUI 프레임워크의 장단점을 비교합니다.

---

## 1. 현재 방식 유지 + `pystray` (Flask + Web Browser + pystray)
현재의 Flask 백엔드와 HTML/JS 프론트엔드를 그대로 유지하면서, Python의 `pystray` 라이브러리를 추가하여 시스템 트레이 아이콘만 띄우는 방식입니다.

*   **장점 (Pros)**
    *   **개발 비용 최소화**: 기존 코드(UI 및 로직)를 거의 100% 재사용할 수 있습니다.
    *   **가벼운 패키징**: 무거운 GUI 라이브러리가 포함되지 않아 빌드 용량이 상대적으로 작습니다.
*   **단점 (Cons)**
    *   **네이티브 앱 경험 부족**: 사용자가 트레이에서 'UI 열기'를 누르면 여전히 기본 웹 브라우저(크롬, 엣지 등)의 새 탭으로 UI가 열립니다. (URL 창 노출 등)
    *   **스레딩/이벤트 루프 충돌**: Flask의 실행 루프와 `pystray`의 트레이 이벤트 루프가 충돌할 수 있어, 스레드 관리가 꽤 까다롭습니다. 앱 종료 시 깔끔한 프로세스 종료 처리가 복잡해질 수 있습니다.

## 2. Webview 라이브러리 도입 (`pywebview` + Flask)
기존 Flask 앱을 유지하되, `pywebview`를 사용하여 크롬 없는(Chromeless) 독립된 데스크톱 창(Windows의 경우 Edge/WebView2 기반)에 웹 UI를 띄우는 방식입니다. 트레이 아이콘 처리는 여전히 `pystray` 등을 병행해야 합니다.

*   **장점 (Pros)**
    *   **기존 UI 재사용**: HTML/CSS/JS 프론트엔드 코드를 그대로 사용할 수 있습니다.
    *   **데스크톱 앱 느낌 부여**: 브라우저 탭이 아닌 독립된 윈도우 창으로 실행되므로 네이티브 앱과 유사한 UX를 제공합니다.
*   **단점 (Cons)**
    *   **트레이 연동의 복잡성**: `pywebview` 창 관리 루프와 트레이 아이콘(`pystray`) 루프를 동시에 안정적으로 돌리는 것이 까다롭습니다. 창을 숨겼다가 트레이에서 다시 클릭했을 때 나타나게 하는 제어가 매끄럽지 않을 수 있습니다.

## 3. PySide6 / PyQt6 (가장 추천하는 네이티브 방식)
Python 진영에서 가장 강력한 데스크톱 GUI 프레임워크인 Qt를 사용하는 방식입니다. 웹 기반이었던 UI를 Qt 위젯(또는 QML)으로 새로 개발해야 합니다.

*   **장점 (Pros)**
    *   **완벽한 트레이 및 윈도우 통합**: `QSystemTrayIcon`이라는 강력한 내장 클래스를 지원하여 Windows 11 트레이 및 알림과 완벽하게 호환됩니다.
    *   **단일 이벤트 루프**: 백그라운드 USB 감지, 트레이 아이콘, UI 창, 이벤트 처리를 Qt의 단일 이벤트 루프(`QApplication`) 안에서 매우 안정적으로 통합 관리할 수 있습니다. 스레드 관리가 훨씬 안전해집니다.
    *   **뛰어난 네이티브 성능 및 디자인**: OS에 가장 잘 녹아드는 네이티브 윈도우 창을 제공합니다.
*   **단점 (Cons)**
    *   **UI 전면 재작성**: 기존 HTML/JS 기반 UI를 버리고 PyQt/PySide 문법에 맞춰 UI를 새로 짜야 합니다. (Flask도 제거됨)
    *   **큰 빌드 용량**: Qt 라이브러리가 포함되어 PyInstaller 빌드 시 결과물(.exe)의 용량이 크게 증가합니다. (약 50MB~100MB 추가)
    *   *(참고)* **라이선스**: PyQt6는 GPL (오픈소스 필수), PySide6는 LGPL (상업적/비공개 사용 제약 적음)이므로 **PySide6** 사용을 권장합니다.

## 4. CustomTkinter (가벼운 네이티브 대안)
Python 내장 GUI인 Tkinter를 모던하게 꾸며주는 `customtkinter`를 사용하고, 트레이는 `pystray`로 처리하는 방식입니다.

*   **장점 (Pros)**
    *   **가벼움**: Qt에 비해 훨씬 가볍고 설치/빌드가 빠릅니다.
    *   **모던한 UI**: 기존 Tkinter와 달리 다크모드 및 세련된 디자인을 쉽게 구현할 수 있습니다.
*   **단점 (Cons)**
    *   **통합 트레이 미지원**: Qt처럼 자체 트레이 기능을 제공하지 않아 `pystray`와 이중으로 이벤트 루프를 관리해야 하는 문제는 (1)번과 동일하게 남습니다.
    *   기존 웹 UI를 새로 작성해야 하는 부담이 존재합니다.

---

## 💡 요약 및 제안

| 비교 항목 | 1. 유지 + pystray | 2. pywebview | 3. PySide6 (추천) | 4. CustomTkinter |
| :--- | :--- | :--- | :--- | :--- |
| **UX (네이티브 느낌)** | 낮음 (웹 브라우저) | 보통 (웹뷰 창) | **매우 높음** | 높음 |
| **트레이 아이콘 안정성**| 스레드 충돌 우려 | 스레드 충돌 우려 | **매우 안정적 (내장)** | 스레드 충돌 우려 |
| **기존 코드 재사용성** | **매우 높음** (웹 그대로) | **높음** (웹 그대로) | 낮음 (UI 전면 재작성) | 낮음 (UI 재작성) |
| **백그라운드 관리 용이성**| 낮음 | 보통 | **매우 높음 (단일 루프)**| 보통 |
| **빌드(exe) 용량** | 작음 | 작음 (OS WebView 의존)| 큼 | 작음 |

### 🏆 최종 제안
앱 전체 수정이 고려될 수 있는 상황이라면, 장기적인 안정성과 데스크톱 앱으로서의 완성도를 위해 **PySide6로의 전면 전환**을 강력히 추천합니다.

1.  **안정성**: 백그라운드 USB 감지(WMI)와 트레이 아이콘, UI 창 열기/닫기가 하나의 안정된 루프에서 동작하므로 프로그램이 꼬이거나 좀비 프로세스로 남을 확률이 획기적으로 줄어듭니다.
2.  **완성도**: "백그라운드 감시 상태 확인 -> 트레이 아이콘 우클릭 -> 설정 창(UI) 띄우기 -> X를 눌러도 종료되지 않고 트레이로 숨기기" 와 같은 전형적인 Windows 백그라운드 유틸리티 동작을 가장 깔끔하게 구현할 수 있습니다.
3.  기존 `camera_handler.py` (핵심 전송 로직)와 `usb_detector.py` (감지 로직 일부)는 그대로 PySide6의 `QThread` 워커로 쉽게 이식할 수 있습니다. 버려지는 것은 오직 HTML과 Flask 라우팅 코드뿐입니다.

---
어느 방향으로 진행할지 검토해 보시고 결정되시면, 해당 방향에 맞추어 `implementation_plan.md`를 작성하여 상세 구현 계획을 승인받도록 하겠습니다.
