# 자동 업데이트 기능 구현 및 트러블슈팅 요약

본 문서는 DSLR File Transfer 앱에 도입된 "자동 업데이트" 기능의 구현 내용과, 배포(PyInstaller) 환경에서 발생했던 주요 이슈 및 해결 방법을 정리한 문서입니다.

## 1. 자동 업데이트 기능 개요

앱 사용자가 개발 지식이 없는 비개발자 환경에서도 원활하게 최신 버전을 유지할 수 있도록, GitHub Release를 통한 자동 업데이트 기능을 구현했습니다.

### 주요 기능
- **버전 체크**: GitHub Repository의 최신 Release 태그(`vX.X.X`)를 확인하여 현재 버전과 비교합니다.
- **업데이트 배너 노출**: 새 버전이 존재할 경우 웹 UI 상단에 업데이트 안내 배너가 표시됩니다.
- **원클릭 업데이트**: 사용자가 `[업데이트]` 버튼을 클릭하면 백그라운드에서 최신 실행 파일을 다운로드하고, 기존 앱을 종료한 뒤 새 버전으로 교체하여 자동 재시작합니다.

## 2. 업데이트 메커니즘 (로직 흐름)

업데이트 과정은 다음과 같은 순서로 진행됩니다.

1. **다운로드**: 최신 버전의 실행 파일(`DSLR_FileTransfer.exe`)을 `update_temp.exe`라는 이름으로 현재 앱 실행 경로에 다운로드합니다.
2. **배치 스크립트 생성**: 교체 작업을 수행할 임시 배치 파일(`update.bat`)을 생성합니다.
3. **환경 변수 초기화 (중요)**: 자식 프로세스로 스크립트를 실행하기 전, 현재 PyInstaller 환경 변수(`_PYI_`, `_MEI`)를 모두 제거합니다.
4. **스크립트 실행 및 앱 종료**: 백그라운드(콘솔 창 숨김)에서 `update.bat`을 실행하고, 기존 Python(Flask) 앱 프로세스를 종료합니다.
5. **교체 및 재시작**:
   - `update.bat`은 원본 `DSLR_FileTransfer.exe`가 완전히 종료될 때까지 대기합니다.
   - 기존 원본 파일을 삭제하고, `update_temp.exe`를 원본 이름으로 변경(Move)합니다.
   - 교체된 최신 버전의 `DSLR_FileTransfer.exe`를 실행한 뒤 배치 파일 자신을 삭제합니다.

## 3. 트러블슈팅: `python312.dll LoadLibrary` 오류

### 문제 현상
업데이트 다운로드 완료 후, 새 버전의 앱이 재시작되는 시점에 `python312.dll LoadLibrary: 지정한 모듈을 찾을 수 없습니다.` 라는 시스템 팝업 오류가 반복해서 발생했습니다. (오류 팝업 창을 닫으면 그제서야 앱이 정상적으로 열리는 현상)

### 원인 분석
PyInstaller의 `One-File` 모드로 빌드된 실행 파일은 런타임에 임시 폴더(`AppData\Local\Temp\_MEIxxxxxx`)에 내부 라이브러리를 압축 해제하고 실행됩니다.
- `app.py` 내부에서 `subprocess.Popen`을 통해 `update.bat`을 실행할 때, 부모 프로세스(기존 앱)의 환경 변수가 자식 프로세스(스크립트 및 새 앱)로 고스란히 상속되었습니다.
- 상속된 환경 변수 중에는 **PyInstaller가 내부적으로 사용하는 `_PYI_APPLICATION_HOME_DIR` 등의 변수**가 포함되어 있었습니다.
- 새 버전의 앱(PyInstaller Bootloader)이 실행될 때, 상속받은 이 환경 변수들을 보고 *"나는 이미 기존 임시 폴더에 압축을 풀었다"* 고 착각하게 됩니다.
- 하지만 `update.bat` 실행 시점에 기존 앱은 이미 종료되어 기존 임시 폴더(`_MEIxxxxxx`)는 삭제된 상태였기 때문에, 새 앱이 삭제된 폴더에서 `python312.dll`을 찾으려다가 오류가 발생한 것입니다.

### 해결 방법
`app.py`의 `apply_update()` 함수에서 `subprocess.Popen`을 호출하기 직전에, `os.environ`을 복사한 후 **`_PYI_` 및 `_MEI`로 시작하는 모든 환경 변수**를 강제로 제거했습니다.
이로 인해 자식 프로세스는 완벽하게 독립적이고 깨끗한 환경에서 시작되며, 새 앱이 정상적으로 고유의 임시 폴더에 압축을 풀게 되어 DLL 로딩 오류가 완전히 해결되었습니다.

```python
# PyInstaller가 설정한 내부 환경변수를 모두 제거하여 자식 프로세스(새 앱)가 완전히 깨끗한 상태로 실행되도록 함
env = os.environ.copy()
for key in list(env.keys()):
    if key.startswith('_PYI_') or key.startswith('_MEI'):
        env.pop(key, None)
```

## 4. 임의의 디렉토리 실행 대응 (CWD 문제 해결)

기존에는 `update_temp.exe`와 `update.bat`을 단순 파일명으로만 참조하여 생성했습니다. 이 경우 사용자가 앱을 바로가기로 실행하거나, 다른 작업 폴더(CWD)에서 실행할 경우 파일이 엉뚱한 위치에 저장되거나 덮어쓰기에 실패할 위험이 있었습니다.

**개선 사항:**
- `sys.executable`을 사용하여 현재 실행 중인 `.exe` 파일의 **절대 경로**를 확보합니다.
- `update_temp.exe` 및 `update.bat`을 원본 실행 파일과 정확히 동일한 폴더에 생성하도록 절대 경로 조합(`os.path.join`)으로 수정했습니다.
- 이로써 바탕화면, USB 등 어느 위치에서 앱을 실행하든 안정적으로 업데이트 파일 교체 및 재시작이 이루어지게 되었습니다.

---
*작성일자: 2026-08-07*
