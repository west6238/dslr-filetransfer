# Project Rules (dslr-filetransfer)

`CHANGELOG.md` 작업 이력을 바탕으로 도출된 본 프로젝트의 주요 작업 및 설계 규칙입니다. 새로운 기능 개발이나 수정 시 아래 원칙을 준수해 주세요.

## 1. 문서화 및 이력 관리 규칙
- **CHANGELOG 관리**: 모든 주요 변경 사항(기능 추가, 버그 수정, 아키텍처 변경 등)과 업데이트 이력은 반드시 `CHANGELOG.md` 파일에 누적하여 기록합니다.
- **버저닝 포맷**: 버전을 명시할 때 시맨틱 버저닝(예: `[v1.1.0] - YYYY-MM-DD`)을 준수하며, `Added (추가됨)`, `Changed (변경됨)` 등으로 범주를 나누어 명확하게 작성합니다.

## 2. 아키텍처 및 시스템 설계 원칙
- **초경량 백그라운드 프로세스**: 하드웨어 이벤트(예: USB 연결 모니터링) 등 항시 동작해야 하는 기능은 별도의 초경량 백그라운드 에이전트(예: `usb_detector.py`)를 두어 시스템 리소스 소모를 최소화합니다.
- **단일 실행 보장 (Single Instance)**: 중복 프로세스 생성을 방지하고, 실행 시나리오에 따라 적절한 모드(백그라운드 모니터링 진입 vs UI 팝업 모드)로 지능적 분기 처리를 구현합니다.
- **스레드 분리**: 백그라운드 루프와 UI(또는 트레이 아이콘) 동작이 서로 멈추지 않도록 스레드를 적절히 분리하여 설계합니다.

## 3. 기기 연결 및 식별 보안
- **고유 식별 체계**: 여러 대의 기기를 다룰 때는 반드시 각 기기의 고유 시리얼 번호(Serial Number) 기반으로 식별하고 개별 관리합니다.
- **미등록 기기 차단 (Whitelist 기반)**: 사전에 등록되지 않은 기기나 타사 장치가 연결되었을 때 앱이 임의로 오동작하지 않도록 차단하는 옵션(예: `autorun_only_registered`)을 기본 활성화 상태로 유지합니다.

## 4. 윈도우 OS 호환성 및 안정성
- **Deprecated 의존성 회피**: 최신 Windows 환경(Windows 11 등)과의 호환성을 위해 `wmic`와 같은 제거 예정이거나 구형인 CLI 명령어 의존성을 지양합니다.
- **공식 API 권장**: 시스템 정보를 조회하거나 이벤트를 감지할 때는 Python의 `win32com` 라이브러리 등을 활용하여 WMI(Windows Management Instrumentation)와 같은 시스템 API를 직접 호출하여 안정성을 확보합니다.

## 5. UI/UX 및 사용자 편의성
- **웹 기반 GUI**: UI는 웹 브라우저 기반의 직관적인 GUI(Flask + Web 등)를 유지 및 발전시킵니다.
- **시스템 트레이 활용**: 백그라운드로 도는 프로세스의 실행 상태를 사용자가 시각적으로 인지하고 제어할 수 있도록 윈도우 시스템 트레이(숨겨진 아이콘)를 지원하며, 직관적인 우클릭 메뉴(UI 열기, 앱 종료 등)를 제공해야 합니다.
- **자동화된 경험**: AutoRun 등록 등 사용자가 수동으로 설정해야 하는 부분은 시작프로그램 등 가장 응답 속도가 빠르고 안정적인 방식을 채택하여 원활한 사용자 경험을 제공합니다.
