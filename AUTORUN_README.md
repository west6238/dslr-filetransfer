# DSLR AutoRun 자동 실행 기능 사용 설명서

이 문서는 Windows 11 환경에서 DSLR 카메라(MTP/PTP 모드 지원 장치)를 USB로 연결했을 때 `usbapp_autorun_v2` 앱이 자동으로 실행되게 하는 설정 방법을 설명합니다.

---

## 1. 자동 실행 기능 설치 (최초 1회)

Windows 시스템의 레지스트리에 이 앱을 정식 '자동 실행 핸들러'로 등록해야 합니다.

1. `usbapp_autorun_v2` 폴더를 엽니다.
2. `autorun_setup.py` 파일을 마우스 우클릭 후 **'Python으로 실행'** 하거나, 명령 프롬프트(CMD)를 관리자 권한으로 열고 아래 명령어를 입력합니다.
   ```cmd
   python autorun_setup.py
   ```
3. 스크립트가 실행되면 사용자 계정 컨트롤(UAC) 확인 창이 뜰 수 있습니다. "예(Yes)"를 누릅니다.
4. 콘솔 화면에 메뉴가 나타나면 **숫자 1을 입력하고 엔터**를 칩니다.
5. `[SUCCESS] AutoPlay 핸들러가 성공적으로 등록되었습니다.` 메시지가 뜨면 설치 완료입니다. 창을 닫아주세요.

---

## 2. 자동 실행 알림에서 앱 선택

등록을 완료한 후, 처음으로 카메라를 USB로 PC에 연결하면 Windows 우측 하단에 알림이 표시됩니다.

1. 카메라를 USB로 연결하고 전원을 켭니다.
2. 우측 하단에 **"이 기기에 수행할 작업 선택"** 이라는 Windows 알림이 나타납니다.
3. 알림을 클릭하면 수행할 수 있는 프로그램 목록이 나옵니다.
4. 목록에서 **"DSLR 사진/비디오 가져오기 (usbapp_autorun_v2)"** 를 클릭하세요.
   *(팁: 클릭하면 해당 앱이 '항상 수행할 작업'으로 자동 고정됩니다.)*

이제 다음번부터 카메라를 연결할 때마다 아무것도 누르지 않아도 **자동으로 앱이 팝업**됩니다!

---

## 3. Nikon 카메라 전용 자동 실행 (필터링)

현재 이 앱은 **Nikon 카메라(하드웨어 ID: `VID_04B0`)** 연결 시에만 창을 띄우고, 다른 스마트폰이나 타사 카메라를 연결했을 때는 알림 없이 백그라운드에서 **자동으로 종료**되도록 설정되어 있습니다.

이 덕분에 귀찮은 오작동을 막을 수 있습니다.

### ※ 타사 카메라에도 띄우고 싶다면?
만약 Nikon 외의 다른 카메라에도 앱이 실행되게 만들고 싶다면, `config.json` 파일을 열고 아래 옵션을 변경하세요.

```json
{
    "restrict_to_nikon": false,
    ...
}
```
(앱을 실행한 뒤 설정(톱니바퀴)을 저장해도 `config.json`이 갱신됩니다. 수동으로 `true`를 `false`로 바꿔주시면 모든 기기에서 반응합니다.)

---

## 4. 자동 실행 기능 해제 (삭제)

더 이상 카메라를 꽂았을 때 앱이 자동 실행되는 것을 원치 않으시면, 설치했던 방식 그대로 깔끔하게 윈도우 레지스트리에서 지울 수 있습니다.

1. `autorun_setup.py`를 실행합니다. (관리자 권한 요청 수락)
2. 메뉴가 나타나면 **숫자 2를 입력하고 엔터**를 칩니다.
3. `[SUCCESS] AutoPlay 핸들러가 성공적으로 삭제되었습니다.` 메시지가 뜨면 삭제가 완료된 것입니다.
