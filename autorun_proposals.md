# Windows 11 MTP/PTP(DSLR 카메라) 장치 연결 시 AutoRun 구현 방안 제안

Windows 11에서 드라이브 문자가 없는 MTP/PTP 장치(DSLR 카메라 등)가 USB로 연결되었을 때, `usbapp_autorun_v2`가 자동으로 실행(AutoRun)되게 하는 3가지 솔루션 제안 문서입니다.

---

## 1안: Windows 표준 '자동 실행(AutoPlay)' 핸들러 등록 (가장 추천 🌟)
Windows의 기본 기능인 "자동 실행" 메뉴에 우리 앱을 정식 사진/비디오 가져오기 프로그램으로 등록하는 표준 방식입니다.

*   **구현 원리**:
    1. 파이썬 스크립트(또는 .reg 파일)로 Windows 레지스트리의 `AutoplayHandlers\Handlers` 경로에 우리 앱의 실행 경로(`.exe` 또는 `.bat`)를 등록합니다.
    2. WPD 장치 연결 이벤트(`WPD\ImageSource`)와 우리 앱을 매핑합니다.
    3. 최초 연결 시 Windows 우측 하단에 "이 기기에 수행할 작업 선택" 알림이 뜹니다. 사용자가 **'usbapp_autorun_v2 - 항상 이 작업 수행'**을 선택하면, 이후부터는 꽂자마자 앱이 켜집니다.
*   **Nikon 전용 처리**: 이 방식은 스마트폰을 꽂아도 켜질 수 있습니다. 따라서 파이썬 앱 런칭 직후 `camera_handler.py`에서 기기명(`device_name`)에 "Nikon"이 없으면 창을 띄우지도 않고 **즉시 조용히 종료(Silent Exit)**하도록 코드를 3줄만 추가하면 깔끔하게 해결됩니다.
*   **장점**: 상용 소프트웨어(Dropbox, Adobe Lightroom 등)가 사용하는 방식과 동일하며, 앱이 항상 켜져있을 필요가 없어 PC 리소스(메모리/CPU)를 전혀 소모하지 않는 가장 표준적이고 안전한 방법입니다.

---

## 2안: Windows 작업 스케줄러 (Task Scheduler) 기반 하드웨어 ID 감지
Windows가 내부적으로 남기는 USB 장치 연결 로그를 감시하여 앱을 강제 실행하는 방법입니다.

*   **구현 원리**: 
    1. Windows '작업 스케줄러'를 엽니다.
    2. 이벤트 트리거를 설정하여 `DriverFrameworks-UserMode` 로그에서 새 장치 연결 이벤트를 감지하게 합니다.
    3. 이때 XML 필터를 사용해 **Nikon의 고유 하드웨어 벤더 ID (VID: `04B0`)**가 연결되었을 때만 `usbapp_autorun_v2`를 실행하도록 예약 작업을 등록합니다.
*   **장점**: 최초 1회 선택 과정조차 필요 없이, 특정 브랜드(Nikon)에만 100% 조건부 자동 실행을 구현할 수 있습니다.
*   **단점**: 사용자 PC마다 작업 스케줄러를 등록하는 권한 및 세팅 과정이 레지스트리 방식보다 다소 까다롭습니다.

---

## 3안: 초경량 백그라운드 감시 데몬 (Python WMI Watcher)
메인 앱과는 별도로 눈에 보이지 않는 백그라운드 감시 스크립트를 만들어 Windows 시작 시 켜두는 방식입니다.

*   **구현 원리**:
    1. `python-wmi` 또는 `win32api`를 이용해 시스템의 장치 변경 이벤트(`WM_DEVICECHANGE`)를 무한 대기하는 아주 가벼운 백그라운드 스크립트(`watcher.py`)를 만듭니다.
    2. USB 플러그인 이벤트가 발생하면 장치 목록을 조회하고 Nikon 장치가 있으면 메인 앱(`app.py`)을 팝업시킵니다.
*   **장점**: 레지스트리나 Windows 설정 건드림 없이 파이썬 생태계 안에서 모든 통제가 가능합니다.
*   **단점**: 아무리 가벼워도 백그라운드 프로세스가 하나 항상 돌고 있어야 하므로 리소스 낭비가 발생할 수 있습니다.

---

## 💡 결론 및 구현 방향 추천
가장 배포하기 쉽고 사용자 경험이 좋은 방식은 **[ 1안: AutoPlay 레지스트리 등록 + 앱 내부 Nikon 기기 필터링 ]** 입니다.
추후 이 방식으로 확정을 결정해주시면, 1회 클릭으로 모든 윈도우 레지스트리를 세팅해주는 설치용 파이썬 스크립트 작성 및 앱 내 필터링 코드를 구현하도록 하겠습니다.
