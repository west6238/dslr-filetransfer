(조건 추가됨) 아래 use-caes 로직 수정-보완 필요한 부분을 다시 검토해줘
```
배포판 (pyinstaller로 제작) executable file을 클릭하면
   - `detector` 모드로 실행 시작 .
   - USB 연결 체크해서 
	- digital camera가 없으면 백그라운드 모니터링 지속
		. `app` 실행 파일을 더블클릭 실행하면, (camera 연결없이) app 화면 띄운다.
		.  app 화면 떠 있는 상태에서도 camera-USB 연결은 모니터링 한다.
		. camera usb 연결되면 사용자 UI 조작에 따라 실행된다.
	- digital camera가 있으면 `detector` 모드 pending... 
		. `app` browser 실행
		. `설정` 에서 기기 등록 신규 / update 지원
	- `app` 실행 중단 / 완료되면, 백그라운드 모니터링 다시 시작
		. `detector`는 사용자 - 의도적인 processor kill 로만 중단된다.


개발 모드에서는 터미널에서 실행한다.
```
