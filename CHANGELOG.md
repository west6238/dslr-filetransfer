# Changelog

모든 주요 변경 사항과 업데이트 이력은 이 파일에 누적하여 기록됩니다.

## [v2.1.0] - 2026-08-17
### Added (추가됨)
- **일반 사용자(End-user) 대상 매뉴얼 제공**: 설치부터 기기 등록, Auto-fetch 등 앱의 주요 기능을 그림 및 예시와 함께 안내하는 HTML 매뉴얼(`manual.html`) 추가.
- **백그라운드 파일 삭제 모듈 추가 (.NET 8 기반)**: WPD(Windows Portable Devices) API를 직접 다루는 커스텀 C# CLI 도구(`wpd_deleter.exe`)를 개발하여, 기존 윈도우 OS의 다이얼로그(네이티브 팝업창) 강제 호출 없이 미디어 파일만 조용히 백그라운드에서 삭제할 수 있도록 지원.
- **자동 실행(AutoRun) 수동 제어 옵션 추가**: 커맨드라인에서 `--add-startup` 및 `--remove-startup` 인수를 전달하여 실행 시, 앱을 구동하지 않고 시작프로그램(Registry) 등록/해제만 안전하게 처리 후 즉시 종료하는 독립 기능 추가.

### Changed (변경됨)
- **UI 구조 및 삭제 워크플로우 개편**: 메인화면의 'Delete Originals' 수동 삭제 버튼을 제거하고, 접이식(Accordion) 구조의 Settings 영역으로 통합.
- **"가져온 다음 항상 장치에서 지우기" 옵션 추가**: Settings 내 체크박스를 통해 파일 가져오기가 끝남과 동시에 백그라운드로 카메라 파일들을 즉시 삭제할지 설정하는 흐름으로 완전 개편.
- **동일 이름 파일 중복 방지 로직 적용**: 파일 복사 중 도착지 폴더에 동일한 이름의 파일이 존재할 경우, 삭제나 덮어쓰기 대신 파일명 끝에 자동으로 순번 꼬리표(예: `DSC_001 - (1).JPG`)를 추가하여 안전하게 저장하도록 개선.
- **완료 알림 UI 및 UX 개선**: 파일 가져오기 및 (선택적) 원본 삭제까지 모두 완료된 후, 명확한 결과 안내 팝업을 띄우도록 변경. 팝업 확인 시 프로그램은 시스템 트레이로 숨겨지면서(Background 유지), 동시에 파일이 저장된 PC 경로의 윈도우 탐색기 창을 자동으로 열어주도록 사용자 편의성 극대화.
- **미등록 기기 UI 방어 로직 추가**: 미등록 기기 연결 시 'Auto-fetch on connect' 및 '가져온 다음 항상 장치에서 지우기' 설정을 비활성화하고 숨김(Hide & Disable) 처리하여 잘못된 설정 저장을 방지.
- **백그라운드 자동 실행(Auto-fetch) 무결성 개선**: USB 연결 감지 후 카메라 내부 스캔 스레드가 완전히 종료된 후에만 자동 다운로드가 실행되도록 Race Condition(경합 조건) 문제를 수정하고, 트레이 모드(Background) 상태에서도 현재 UI의 최신 설정값(태그명, 경로 등)을 올바르게 반영하도록 로직 개선.
- **프로젝트 규칙(RULES.md) 보완**: 빌드 및 배포 시 생성되는 압축 파일(`.zip`)의 저장 위치를 `dist` 폴더로 고정하는 규칙 추가.
- **메인 UI 레이아웃 및 동작 단순화**: `Scan Files` 버튼을 제거하고, 카메라가 연결되면 별도 조작 없이 즉시 백그라운드 스캔이 시작되도록 자동화. 기존 아코디언(접이식) 형태였던 `Settings` 패널을 항상 열림(고정) 상태로 변경하고, 스캔 진행 상황(`Progress`) 아래로 배치하여 시각적 인지 흐름(Status -> Progress -> Settings)을 개선.

### Fixed (수정됨)
- **자동 파일 지우기 실패 버그 수정**: "Auto-fetch on connect" 흐름에서 삭제 모듈(`WpdDeleter.exe`) 실행 시, 카메라 COM 객체(Lock) 해제 시점과 맞물려 삭제가 진행되지 않고 실패하던 이슈(Locking 및 대소문자 불일치)를 수정.
- **배포판(PyInstaller) WpdDeleter.exe 실행 실패 버그 수정**: `--onedir` 형태로 패키징된 배포 버전 실행 시, 내부 에셋 경로(`_MEIPASS`)가 다르게 해석되어 백그라운드 원본 지우기 도구를 찾지 못하던 문제를 수정.
- **MTP 및 Mass Storage 스캔 안정성 개선**: 백그라운드 스레드에서 장치 스캔 시 첫 번째 항목을 무조건 선택하여 발생하는 기기 인식 오류(예: OneDrive 스캔 등)를 해결하기 위해 `device_name`을 정확히 매칭하도록 로직 변경. MTP 기기 중 일부 접근 불가 폴더에서 예외가 발생하더라도 전체 탐색이 중단되지 않도록 `try-except` 위치 최적화.
- **백그라운드 오류 UI 피드백 연동**: `scan_failed`, `fetch_failed` 시그널을 메인 UI 핸들러에 연결하여 에러 발생 시 무한 "스캔 중..." 텍스트에 머무는 문제 수정.

### Optimized (최적화)
- **배포판 용량 최적화**: PyInstaller 빌드 시 PySide6 프레임워크의 앱 구동과 무관한 거대한 Qt 바이너리 파일(예: `opengl32sw.dll`, `Qt6Qml.dll`, `Qt6Pdf.dll` 등)을 `.spec` 파일에서 수동으로 필터링 및 제외하여 배포판 파일 크기를 크게 축소.

### Pending Issues (알려진 문제 / 보류)
- **MTP 장치 스캔 및 파일 가져오기 이슈**: MTP와 Mass Storage가 복합적으로 구성된 환경이거나, MTP로 연결된 특정 카메라 환경에서 `Auto-fetch`, `file-scan`, `file-fetch` 가 정상 작동하지 않는 이슈 확인. (상세 원인 및 해결 방안은 추후 분석)

> **[Notice]** 현재 코드는 `v2.1.0` 버전으로 고정(Fix)되었으며, 이후 진행되는 기능 개선이나 버그 수정 작업부터는 앱 버전(`APP_VERSION`)이 수정/상향될 예정입니다.

## [v2.0.0] - 2026-08-16
### Added (추가됨)
- **앱 자동 업데이트 연동 로직 추가 (v2.0.0 버전 기준)**: GitHub Releases API(`releases/latest`)를 통해 4시간 간격으로 주기적으로 최신 버전을 확인하고, 업데이트가 있을 시 UI에 알림을 띄워 원클릭으로 `.zip` 다운로드, 파일 덮어쓰기(`updater.bat`) 및 자동 재시작이 되도록 지원하는 기능 구현.
- **시작프로그램(Startup) 레지스트리 로직 비활성화**: 앱을 처음 실행할 때 시작프로그램에 등록할지 묻는 기능을 비활성화 처리. (추후 사용자 요구(VOC) 시 재검토 예정)
- **프로젝트 규칙 문서 추가**: 기존 작업 이력을 기반으로 도출된 설계 및 작업 원칙을 정리한 `RULES.md` 파일 생성.
- **GUI 프레임워크 분석 추가**: Windows 11 시스템 트레이 앱 전환을 위한 데스크톱 GUI 프레임워크 장단점 분석 및 제안 문서(`gui_framework_comparison.md`) 작성.

### Changed (변경됨)
- **버전 번호 관리체계 확립**: 이번 데스크탑 네이티브 GUI 전환부터 기준 버전을 `v2.0.0`으로 재정립하며, 앱 메인 진입점 내부에 앱 버전을 코드화(`APP_VERSION`)하여 상태 패널 영역에 노출되도록 개선.
- **GUI 프레임워크 PySide6 기반으로 전면 교체**: 시스템 트레이 팝업 및 백그라운드 프로세스 등 데스크탑 네이티브 환경에 최적화하기 위해 기존 Web/Flask 기반에서 Qt(PySide6) 프레임워크 기반의 앱(`app/main.py`)으로 구조 전환.
- **Settings UI 개편 및 사용성 향상**: Tag Name과 저장 폴더 항목에 과거 사용 이력을 드롭다운에서 바로 선택할 수 있는 `QComboBox` 위젯 적용 및 시스템 네이티브 `Folder Selector` 기능 도입.
- **설정값 변경 추적 및 오동작 방지 (Dirty State)**: 사용자가 UI에서 설정값을 수정하고 `설정 (Save Settings)`을 누르지 않은 상태에서 스캔, 복사 등의 액션을 수행하려 할 경우 경고 팝업을 띄우고 실행을 차단하는 안전장치 마련.
- **Windows 기본 폴더 경로 동적 인식**: 저장 경로 기본값을 하드코딩된 사용자 폴더 경로 대신, `win32com` Shell API를 통해 운영체제에 설정된 실제 '사진(Pictures)' 폴더 위치를 추적하도록 변경.

### Security (보안 강화)
- **미등록 기기 Auto-fetch 제어 메커니즘**: Auto-fetch 옵션이 활성화되어 있더라도 앱에 등록(Whitelist)되지 않은 기기가 연결될 경우 자동 데이터 다운로드 로직을 무시하고, 미등록 알림 팝업을 띄워 사용자 데이터를 보호.

## [v1.1.0] - 2026-08-11
### Added (추가됨)
- **백그라운드 에이전트(Detector) 도입**: USB 이벤트(WMI)를 모니터링하여 카메라 연결 시 앱을 자동 실행하는 초경량 백그라운드 프로세스(`usb_detector.py`) 추가.
- **다중 기기 등록 기능**: 여러 대의 카메라를 고유 시리얼 번호(Serial Number) 기반으로 식별하고 개별 등록/관리할 수 있는 기능 추가.
- **미등록 기기 무시 기능**: 사전에 등록되지 않은 기기나 타사 장치를 연결했을 때 앱이 임의로 실행되지 않도록 차단하는 `autorun_only_registered` 옵션(기본값: 활성화) 추가.
- **단일 실행(Single Instance) 지능형 제어**: 앱 실행 시 중복 프로세스 생성을 방지하고, 첫 번째 클릭 시 백그라운드 모니터링 모드 진입 / 두 번째 클릭 시 UI 팝업 모드로 지능적 분기 처리되도록 진입점 개편.
- **Windows 11 최신 호환성 패치**: 백그라운드 프로세스 감지 로직에서 제거(Deprecated)된 `wmic` 명령어 의존성을 없애고, Python `win32com` 라이브러리로 직접 WMI를 쿼리하도록 수정하여 안정성 확보.
- **시스템 트레이(숨겨진 아이콘) 지원 기능 추가**: 백그라운드 프로세스의 실행 상태를 윈도우 시스템 트레이에서 시각적으로 확인하고, 우클릭 메뉴를 통해 수동으로 'UI 열기' 및 '앱 종료'를 제어할 수 있도록 구현. (`pystray` 패키지 적용, 스레드 분리 아키텍처 개편)

### Changed (변경됨)
- **자동 실행(AutoRun) 등록 방식 변경**: 기존 윈도우 시스템 레지스트리의 `AutoPlay` 쉘 커맨드를 이용하던 방식에서, 시작프로그램(`HKCU\...\Run`)에 전용 에이전트를 등록하는 방식으로 변경하여 응답 속도 및 안정성 향상.
- **설정 화면 UI 전면 개편**: 기존 AutoPlay 버튼을 대체하여, 백그라운드 에이전트 실행 상태 표시 및 현재 연결된 기기를 즉시 등록/삭제할 수 있는 '기기 관리 패널' 추가 (`index.html`, `app.js`).

## [v1.0.0] - 이전 릴리즈
### Added
- 기본 PTP/MTP 프로토콜 카메라 연결 및 사진 파일 가져오기 기능.
- 태그명 기반 폴더 자동 생성 및 파일 복사/이동 로직.
- 웹 브라우저 기반 직관적인 GUI 구현 (Flask + Web).
- GitHub Releases 연동 앱 자동 업데이트 다운로드 및 적용 스크립트.
